
## Визуализатор и Анализатор Логических Функций (Boolean Function Analyzer)

Этот проект представляет собой **WPF-приложение**, разработанное для интерактивного изучения, анализа и верификации булевых функций. Инструмент позволяет задавать функции как по их **десятичному номеру**, так и с помощью **логических формул**, автоматически генерируя таблицы истинности, совершенные дизъюнктивные и конъюнктивные нормальные формы.

---

### Основные возможности

Функционал приложения структурирован по трём основным вкладкам для всесторонней работы с булевыми функциями:

**По номеру (Decimal Mode):** Эта вкладка позволяет построить функцию, её **Таблицу Истинности** и **нормальные формы** на основе **десятичного номера функции** ($num$) и заданного количества переменных ($n$).

**По формуле (Formula Mode):** Содержит интегрированный **мини-интерпретатор**, который разбирает логическую формулу и генерирует на её основе Таблицу Истинности, а также совершенные ДНФ и КНФ.

**Сравнение (Comparison Mode):** Позволяет проверить **эквивалентность** двух функций (A и B), заданных любым из двух способов. В случае неэквивалентности приложение находит и отображает **первое контр-слово** (кортеж), на котором значения функций расходятся.

**Поддерживаемые логические операции:**

Приложение поддерживает ввод формул со следующими операторами, которые обрабатываются парсером и автоматически разворачиваются к базису $\{\neg, \land, \lor\}$: **НЕ** (`!` или `not`), **И** (`&` или `and`), **ИЛИ** (`|` или `or`), **XOR** (`^`), **Импликация** (`->`) и **Эквиваленция** (`=`).

---

### Архитектура

Проект построен на архитектурном шаблоне **MVVM** (Model-View-ViewModel) с использованием **C#** и **WPF** (.NET).

**Слой View (WpfAppBoolTable)** отвечает за пользовательский интерфейс, обработку событий ввода и **отображение результатов**.

**Слой Бизнес-логики (Core Services)** включает ключевые математические и аналитические компоненты:
* **`TruthTable`**: Отвечает за **построение таблицы истинности** из десятичного номера или функции-делегата.
* **`DnfGenerator`/`KnfGenerator`**: Классы, ответственные за **генерацию совершенных нормальных форм (СДНФ и СКНФ)**.
* **`FunctionComparer`**: Сервис для **построчного сравнения** двух Таблиц Истинности и поиска контр-слова.
* **`Lexer`, `ParserRpn`, `ExpressionEvaluator`**: Модули, выполняющие **синтаксический анализ формулы**, преобразование её в **Обратную Польскую Запись (ОПЗ)** и последующее **вычисление значения**.

---

### Как использовать

1.  Запустите исполняемый файл приложения (`WpfAppBoolTable.exe`).
2.  Перейдите на нужную вкладку: введите количество переменных $n$ (рекомендуется $n \le 5$) и номер функции или введите логическое выражение (например, `(x1 | x2) -> x3`).
3.  Для сравнения задайте обе функции и нажмите «Эквивалентны?».
4.  Результаты будут отображены в соответствующих полях. Приложение оптимизировано для работы с числом переменных $n \le 5$ из-за экспоненциальной сложности вычислений $O(2^n)$.

---

### 🖥️ Системные требования

Для запуска приложения требуется операционная система **Windows 10 / Windows 11** и установленный **.NET Framework** (версия 4.7.2 или выше, предпочтительно .NET 8). Требования к оборудованию минимальны: процессор 1 ГГц или выше, 1 ГБ оперативной памяти и около 50 МБ свободного места на диске.
